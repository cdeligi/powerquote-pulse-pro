import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function BOMDisplay() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>BOM Display</CardTitle>
      </CardHeader>
      <CardContent>
        <p>BOM Display functionality will be available soon.</p>
      </CardContent>
    </Card>
  );
}